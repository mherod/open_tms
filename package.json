{
  "name": "open_tms",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "backend",
    "frontend",
    "packages/*"
  ],
  "scripts": {
    "build": "npm -w packages/shared run build && npm -w backend run build && npm -w frontend run build",
    "dev": "concurrently -k -n backend,frontend -c blue,green \"npm -w backend run dev\" \"npm -w frontend run dev\"",
    "lint": "eslint .",
    "format": "prettier --write .",
    "clean": "rm -rf node_modules package-lock.json frontend/node_modules backend/node_modules packages/*/node_modules",
    "reinstall": "npm run clean && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "eslint": "^9.9.0",
    "prettier": "^3.3.3",
    "typescript": "^5.6.2",
    "typescript-eslint": "^8.57.2"
  }
}
